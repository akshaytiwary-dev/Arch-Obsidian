# Cava configuration for top bar visualizer (10 bars)
[general]
framerate = 30
bars = 10

[input]
method = pulse

[output]
method = raw
raw_target = /dev/stdout
data_format = ascii
ascii_max_range = 8
